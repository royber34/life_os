# <PLACEHOLDER: project-name>: project CLAUDE.md

<!-- This file lives at the project root, e.g. ~/Repositories/<your-project>/CLAUDE.md.
     Claude Code auto-loads it when the working directory is inside this tree.
     Keep it focused on what's specific to THIS project. Cross-cutting habits live
     in the global ~/.claude/CLAUDE.md; don't repeat them here. Target: under 100 lines. -->

## Orientation
<PLACEHOLDER: 2-3 sentences describing what this project is and what state it's in.>
Example shape: "This is the infrastructure repo for `<service-name>`. It provisions <X> via <Y>, with deployment runbooks in `<path>`. Currently in production; changes here affect live traffic."

## Folder structure
```
<your-project>/
├── src/                    <!-- application code -->
├── infra/                  <!-- IaC definitions -->
│   ├── terraform/
│   └── ansible/
├── scripts/                <!-- one-off + maintenance scripts -->
├── docs/
│   ├── architecture.md     <!-- system diagram + decisions -->
│   └── runbooks/           <!-- operational procedures -->
├── tests/
└── CLAUDE.md               <!-- this file -->
```

<!-- The cross-reference table is the most useful section in a project CLAUDE.md.
     Each row points at a real file Claude should read when the topic comes up.
     This keeps THIS file short; content lives in the linked files. -->
## Key cross-references

| When working on...           | Read first                                  |
| ---------------------------- | ------------------------------------------- |
| Architecture / system design | `docs/architecture.md`                      |
| Deployment / rollback        | `docs/runbooks/deploy.md`                   |
| Incident response            | `docs/runbooks/incident.md`                 |
| Environment variables        | `.env.example` + `docs/env-vars.md`         |
| Database migrations          | `infra/migrations/README.md`                |
| Local development setup      | `README.md` (Quick start section)           |
| Testing conventions          | `tests/README.md`                           |
| API contract                 | `<PLACEHOLDER: openapi.yaml or similar>`    |

## Conventions
- **Branching:** `main` is protected. Work on `<PLACEHOLDER: branch-prefix>/<topic>` branches.
- **Commits:** <PLACEHOLDER: e.g. "Conventional commits. Sign all commits.">
- **Tests:** Run `<PLACEHOLDER: test-command>` before any push. CI runs the same on PR.
- **Secrets:** Never commit secrets. `.env` is gitignored; use `.env.example` as the template.
- **Dependencies:** <PLACEHOLDER: e.g. "Pin exact versions. Update via dependency PRs only.">

## Do-not rules (project-specific)
- **Do not modify `<PLACEHOLDER: generated-file-path>`.** It's generated by `<PLACEHOLDER: generator>`. Regenerate instead.
- **Do not run migrations against `<PLACEHOLDER: env-name>` from a local machine.** Use the CI deploy job.
- **Do not bump <PLACEHOLDER: critical-dependency> without coordinating.** It has known breaking changes between minor versions.
- **Do not delete files under `<PLACEHOLDER: protected-path>` without checking with me.** Likely in-progress work.

## Where to find more
- Project README: `./README.md` (overview, setup, deployment)
- Architecture decisions: `./docs/adr/` (one file per decision)
- Past incidents: `./docs/incidents/` (postmortems and root-cause notes)
